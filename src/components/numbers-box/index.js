import React from 'react';
import CheckBox from "../Checkbox";

const NumbersBox = () => {
    return (
        new Array(12).map(() => {
            return <p>sdfg</p>;
        })
        // <CheckBox label=" "/>
    );
};

export default NumbersBox;